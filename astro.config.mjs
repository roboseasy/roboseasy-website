// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://roboseasy.netlify.app',
  // Phase 0: Astro 빌드 파이프라인 검증용 최소 설정.
  // 페이지 마이그레이션은 Phase 1+에서 진행.
});
